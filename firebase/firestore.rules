rules_version = '2';
// Browsers never touch the database: the Dineri server reads and writes it with its own key
// (platform/server/src/accounts). Everything a browser asks for directly is refused.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
