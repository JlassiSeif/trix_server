# build output
archive/server-cpp/server
archive/client-sdl/prog
*.o
a.out

# node / TypeScript
node_modules/
dist/
*.tsbuildinfo
e2e-out/
.station-runs/
.data/
.deploy/

# secrets (the Firebase server key lives here locally; on the box it is mounted, never in git)
.secrets/
firebase-debug.log
firestore-debug.log
